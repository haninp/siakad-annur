{
  "name": "siakad-annur",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sistem Informasi Akademik & Keuangan Pesantren An-Nuur Limo",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "mulai": "node scripts/mulai.ts",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db": "npm run build && node scripts/basis-data.ts",
    "db:isi": "npm run build && node scripts/basis-data.ts --isi",
    "db:jelajah": "sqlite3 \"${SIAKAD_DB:-data/sqlite/siakad.db}\"",
    "docker:bangun": "docker compose -f infra/compose.yaml build",
    "docker:db": "docker compose -f infra/compose.yaml run --rm --build migrasi",
    "docker:db:isi": "docker compose -f infra/compose.yaml run --rm --build migrasi node scripts/basis-data.ts --isi",
    "docker:jelajah": "docker compose -f infra/compose.yaml run --rm sqlite",
    "selesai": "npm run build && npm run lint && npm test && node scripts/selesai.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^4.1.10"
  }
}
